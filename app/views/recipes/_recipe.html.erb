<div id="<%= dom_id recipe %>">
  <div>
    <%= link_to recipe.name, user_recipe_path(user_id: current_user.id, id: recipe.id)%>
    <%= button_to "Remove", user_recipe_path(user_id: current_user.id, id: recipe.id), :method => :delete %>
  </div>
  <div>
    <%= recipe.description %>
  </div>
</div>
